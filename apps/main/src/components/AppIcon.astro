---
interface Props {
  iconClass: string;
  size?: number;
}
const { iconClass, size = 46 } = Astro.props;
---

<div class:list={["app-icon", iconClass]} style={`width:${size}px;height:${size}px`} aria-hidden="true">
  <span></span>
</div>
